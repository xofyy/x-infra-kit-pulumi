# ===========================================
# x-infra-kit Pulumi Configuration
# ===========================================
# This file shows environment variables that can be used
# as an alternative to Pulumi config. Copy to .env and update.
#
# Note: Pulumi config (pulumi config set) is the recommended approach.
# Environment variables are supported for CI/CD pipelines.

# ===========================================
# GCP Configuration (Required)
# ===========================================

# GCP Project ID
PULUMI_CONFIG_GCP_PROJECT=your-gcp-project-id

# GCP Region (optional, default: europe-west1)
# Allowed: europe-west1, europe-west3, europe-west4, us-central1, us-east1
PULUMI_CONFIG_GCP_REGION=europe-west1

# ===========================================
# Application Configuration
# ===========================================

# Environment: dev, staging, prod (default: dev)
PULUMI_CONFIG_ENV=dev

# Resource prefix for naming convention (required)
PULUMI_CONFIG_PREFIX=myapp

# ===========================================
# Optional: Secrets and Workload Identity
# ===========================================

# Comma-separated list of secrets to create
# PULUMI_CONFIG_SECRET_IDS=db-password,api-key

# Workload Identity Service Account ID
# PULUMI_CONFIG_WI_SA_ID=myapp-workload-sa

# Kubernetes namespace for workload identity
# PULUMI_CONFIG_WI_NAMESPACE=default

# Kubernetes ServiceAccount name
# PULUMI_CONFIG_WI_SA_NAME=myapp-sa

# ===========================================
# Optional: Cluster Overrides
# ===========================================

# Maximum nodes in the cluster (1-50)
# PULUMI_CONFIG_MAX_NODES=5

# Machine type (e.g., e2-medium, n2-standard-4)
# PULUMI_CONFIG_MACHINE_TYPE=e2-medium

# ===========================================
# Labels for cost tracking
# ===========================================

# Custom labels (JSON format)
# PULUMI_CONFIG_LABELS='{"team":"platform","cost-center":"engineering"}'
